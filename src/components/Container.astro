---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
  id?: string;
}

const { as: Tag = 'section', class: className = '', id } = Astro.props;
---

<Tag id={id} class={`shell ${className}`.trim()}>
  <slot />
</Tag>
